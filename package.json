{
  "name": "template-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "npm install && npm run typings",
    "test": "tsc src/test/test.ts --outDir dist --sourceMap && cd ./dist/test && mocha",
    "build": "tsc src/index.ts --outDir dist --sourceMap --watch",
    "typings": "typings install",
    "static": "static ./dist"
  },
  "author": "shule517",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^3.0.2",
    "node-static": "^0.7.8",
    "power-assert": "^1.4.1",
    "source-map-support": "^0.4.2",
    "typings": "^1.3.2"
  }
}
